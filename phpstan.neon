
includes:
    - ./vendor/nunomaduro/larastan/extension.neon

parameters:
    paths:
        - app

    # Determines the level of checking. 5 is a good starter point
    level: 5

    ignoreErrors:
        - '#Call to method [^\s]+\(\) on an unknown class acidjazz\\metapi\\MetApi#'

    excludePaths:

    checkMissingIterableValueType: false
